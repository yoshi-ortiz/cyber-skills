---
purpose: the spec/ tree `init` writes into a project
admits: CONTEXT, CONTRACTS, WORKFLOWS templates for the project's design harness
refuses: skill-development contracts -- those live beside the code they govern
max_file_bytes: 8000
---

# Project spec templates

Mirror the layout `init` produces. `CONTEXT.md.tmpl` is the project's own context
contract: it tells a fresh agent to read `DECISIONS.md` before proposing anything.
