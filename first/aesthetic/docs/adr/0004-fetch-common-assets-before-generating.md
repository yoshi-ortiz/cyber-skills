# Fetch common assets before generating

The agent reuses project assets or fetches pinned open-source assets before procedural generation. Procedural work is reserved for genuinely custom needs or a bounded search with no suitable result, and it must use a named deterministic generator. This prevents invented vector paths, traced bitmap fonts, and expensive reinvention of common icons, ornaments, kaomoji, and ASCII graphics.
