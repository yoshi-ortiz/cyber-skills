# Save and validate theme elements

Saved companion themes live in the project specification. Each color or font role validates independently before it becomes active, and an unsafe role returns to inference while the companion keeps its last safe value. This allows art direction to shape the companion without letting one white-on-white pairing discard valid work or make the interface unreadable.
