interface:
  display_name: "Bootstrap Design Harness"
  short_description: "Create bounded, evidence-backed design workflows"
  default_prompt: "Create a portable design harness for this project using a caller-selected read-only inspiration source."
