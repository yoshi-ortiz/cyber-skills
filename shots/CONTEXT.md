---
purpose: rendered output from a design project run here, and the screenshots taken of it
admits: generated SVG and the PNG captures of a rendered screen
refuses: hand-edited vectors, source imagery, anything a gate has not passed
---

# Shots

`landing.hero.flow.svg` is written by `aesthetic/scripts/text_to_graphics.py
build` and must pass `gate` before it is wired into anything. Nothing here is
authored by hand; regenerate instead of editing.

No `max_file_bytes`. Screen captures are large by nature.
