---
purpose: the build family router for Code, Build, and Test
admits: SKILL.md, and any reference this family alone needs
refuses: reimplementations of the public skills it routes, and doctrine another family owns
max_file_bytes: 8000
---

# build

Routes the Code, Build, and Test stop on the rail. [SKILL.md](SKILL.md) owns
the order and the handoff; `ponytail`, `tdd`, `test-driven-development`,
`code-review`, `verification-before-completion`, and `semgrep` arrive through
the harness collection and do the work.

Enters with a contract accepted by `first`. Leaves for `land` only after the
Pre-release section has run.
