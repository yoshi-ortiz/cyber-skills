---
purpose: rendered comps from a design project run here
admits: HTML shells for one project's screens, and the article the harness publishes
refuses: skill payload, reusable templates, anything a consuming agent installs
---

# Design

Work product, not skill payload. Fog. `landing-flow-hero.html` embeds
`../shots/landing.hero.flow.svg`; regenerate the SVG rather than editing the
markup around it.
