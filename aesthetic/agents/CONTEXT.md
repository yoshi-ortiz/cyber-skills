---
purpose: per-agent adapter manifests, so all agents resolve to one skill copy
admits: one manifest per agent runtime
refuses: skill logic of any kind -- a manifest that behaves is a fork
max_file_bytes: 4000
---

# Agents

Canonical is the iCloud skill copy. After editing, synchronize the runtime copy
and install the companion. Never patch a runtime copy alone.
