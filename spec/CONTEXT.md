---
purpose: one design project's harness state, kept out of the skill it was produced by
admits: a single `design-harness/` directory of that project's recorded state
refuses: skill contracts, repository specification, anything named in root `SPEC.md`
---

# Spec

Fog, and easy to confuse with root `SPEC.md`. That file is the repository's
contract. This directory is one project's evidence, and Design-Inference
Context reads it while Repo-Dev Context does not.
