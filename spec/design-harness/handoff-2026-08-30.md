# Handoff: landing hero graphics (`text-to-graphics`)

**Saved:** 2026-08-30  
**Project:** `/Users/bebote-pro/Development/cyber-skills`  
**Next session goal:** Run the `text-to-graphics` pipeline and ship a **website landing page hero ASAP**.

---

## User intent (this session)

Continue from the half-progress reference:

| Asset | Path | Status |
|-------|------|--------|
| **Primary reference** | `moodboards/llm-shots/good room space, wrong roads.png` | **Half progress** — good layout, good cartoon style, good room scale; **rooms still incorrect** (inventory fidelity); **road topology wrong** |
| Layout/composition | `moodboards/llm-shots/clear layout.png` | Good layout; rooms undersized |
| Style (cartoon) | `moodboards/cartoon/` | pursue |
| Style (isometric) | `moodboards/isometric cartoon/` | pursue |
| Wireframe | `moodboards/wireframe sketches/Group 47.png` | composition sketch |
| **Avoid** | `moodboards/llm-shots/best ugly shot.png` | tagged `avoid` — monochrome, bad roads, ugly cartoons |

**Deliverable:** Landing page hero for element `landing.hero.flow` — SVG + PNG wired into existing landing HTML.

---

## What was built (do not re-litigate)

A **Design-Inference** loop skill **`text-to-graphics`** under `aesthetic/` (not a top-level skill yet).

### Key artifacts

| Path | Purpose |
|------|---------|
| `aesthetic/references/text-to-graphics.md` | Loop doctrine |
| `aesthetic/scripts/text_to_graphics.py` | CLI: `init`, `observe`, `seed-tags`, `compile`, `export-avge`, `gate`, `moodboard` |
| `aesthetic/scripts/test_text_to_graphics.py` | 4 unit tests (green) |
| `spec/design-harness/graphics-manifest.json` | Corpus → adapters → outputs |
| `spec/design-harness/scene-spec.json` | Geometry truth: 6 rooms, infinity road sequence, billboards |
| `spec/design-harness/corpus.json` | 18 items observed from `moodboards/` |
| `spec/design-harness/corpus-tags.json` | Seeded tags; ugly shot = avoid |
| `moodboards/llm-shots/prompts/slices/avge-calls.json` | **13 AVGE pattern calls** (road + 6 boxes + 6 billboard attaches) |
| `moodboards/llm-shots/prompts/graphics-prompt.json` | Split adapter prompts (style ≠ geometry) |
| `moodboards/storytelling/rooms-inventory.md` | Inventory truth — compile to scene-spec, never paste raw into image models |

### Pipeline command

```bash
cd ~/Development/cyber-skills
python3 aesthetic/scripts/text_to_graphics.py --project-root . init   # already run once
python3 aesthetic/scripts/text_to_graphics.py --project-root . gate   # RED until SVG exists
```

### Architecture decisions (locked)

1. **Raster gen (agy `generate_image`) is moodboard-only** — never final delivery. Failed with HTTP 500 on long prompts.
2. **Final assets = procedural AVGE SVG → PNG**, optional SVGMaker for vector fills.
3. **No prompt contamination** — inventory and style are separate adapter slices.
4. **Image models:** Nano Banana 2 = `gemini-3.1-flash-image-preview`; Pro fallback = `gemini-3-pro-image-preview`.

### MCPs needed (not yet in Cursor config)

- **AVGE Engine** — `isometric_box`, `attach`, `create_line_pattern` → execute `avge-calls.json`
- **SVGMaker** — `style: isometric`, `svgmaker_convert` for reference PNGs

---

## Current blocker

`gate` fails: **`shots/landing.hero.flow.svg` does not exist.**

AVGE MCP has not been run against `moodboards/llm-shots/prompts/slices/avge-calls.json`.

---

## Recommended next-agent sequence (ASAP hero)

### 1. Install / preflight MCPs

Add AVGE Engine + SVGMaker to Cursor MCP config. Record in `spec/design-harness/support.json` per `aesthetic/references/platform-support.md`.

### 2. Build geometry (deterministic)

Agent instruction:

> Execute every call in `moodboards/llm-shots/prompts/slices/avge-calls.json` via AVGE Engine MCP. Render to `shots/landing.hero.flow.svg` and `shots/landing.hero.flow.png`. Fix road topology per `scene-spec.json` infinity sequence — this is what `good room space, wrong roads.png` got wrong.

Use `good room space, wrong roads.png` for **room scale and cartoon tone**, not road shape. Use `clear layout.png` for **composition**. Do not use `best ugly shot.png`.

### 3. Style pass (optional v0.1)

SVGMaker `convert` on pursue-tagged cartoon refs if AVGE output needs illustration density. Inventory characters come from scene-spec + rooms-inventory clusters, not one-shot LLM paste.

### 4. Gate

```bash
python3 aesthetic/scripts/text_to_graphics.py --project-root . gate
```

Must pass: scene-spec valid, SVG exists, six billboard strings present.

### 5. Wire landing page

Existing landing shells:

- `design/landing-flow-hero.html`
- `design/landing-flow-hero-proposal-01.html`

Prior hero shots (may be stale):

- `shots/landing.hero.flow.desktop.png`
- `shots/landing.hero.flow.mobile.png`
- `shots/landing.hero.flow.narrow.png`

Replace hero `<img>` or inline SVG with new `shots/landing.hero.flow.svg` (preferred) or PNG. Match 16:9 desktop + responsive crops if time.

### 6. Do NOT

- Paste `Room Inventory Write.md` (26KB) into agy `generate_image` — 500 errors
- Mix art prompt + rooms inventory in one model call
- Ship agy raster as final hero
- Use `best ugly shot.png` as style reference

---

## Corpus tag summary (quick reference)

| sha256 prefix | note | stance |
|---------------|------|--------|
| `a35fbe91…` | good room scale; road topology fail | pursue |
| `4e0e61d8…` | good layout; rooms undersized | pursue |
| `b07dd736…` | (best ugly shot) | **avoid** |

Full tags: `spec/design-harness/corpus-tags.json`

---

## Scene spec highlights

- **Element:** `landing.hero.flow`
- **Road:** horizontal infinity, clockwise `/first → /build → /land → /check → /first`
- **Billboards:** `/first SKETCH`, `/build CODE`, `/land DEPLOY`, `/check REVIEW`, `/kit EQUIP`, `/fix REPAIR`
- **Inventory ref:** `moodboards/storytelling/rooms-inventory.md`

Full spec: `spec/design-harness/scene-spec.json`

---

## agy CLI (moodboard only)

```bash
python3 aesthetic/scripts/text_to_graphics.py --project-root . moodboard --dry-run
# live (non-deliverable):
python3 aesthetic/scripts/text_to_graphics.py --project-root . moodboard
```

Binary: `~/.local/bin/agy`. Image model baked into `generate_image` as Nano Banana 2.

---

## Suggested skills (call Skill tool)

| Skill | When |
|-------|------|
| **`aesthetic`** | Design-Inference context; read `SKILL.md` + `aesthetic/references/text-to-graphics.md` before any graphics work |
| **`frontend-design`** | Landing hero layout, responsive hero section, wire SVG/PNG into HTML |
| **`gstack/design-html`** | If polishing `design/landing-flow-hero.html` to production quality |
| **`context7-mcp`** | SVG.js / Konva export if browser preview path (v0.2) |
| **`ponytail`** | Keep scope minimal — ship AVGE SVG + one HTML swap first |

---

## Open items (post-hero)

- v0.2: `design/preview.html` with SVG.js transparent PNG export
- Extend `avge-calls.json` with inventory vignettes (currently geometry + billboards only)
- Refresh `corpus.json` after adding new shots to `moodboards/llm-shots/`
- Promote `text-to-graphics` to public skill surface in `SKILL_SPEC.md` when stable

---

## Repo context

- Design-Inference reads `aesthetic/CONTEXT.md` — **not** root `ROADMAP.md`/`SPEC.md` for design decisions
- `moodboards/`, `spec/design-harness/`, `shots/` are **fog** (dev-only, not published)
- Gates: `python3 tools/check.py` (repo-wide); graphics gate is separate CLI above

---

*End handoff.*
