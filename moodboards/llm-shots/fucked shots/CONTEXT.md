---
purpose: generated reference the designer rejected, kept as `avoid` evidence
admits: generated images that were ranked down
refuses: anything accepted, anything unranked
max_file_bytes: 300000
---

# Rejected generations

Same argument as `shots/fucked/`, one layer up the pipeline: the rejection is
the signal. Deleting it means regenerating it.
