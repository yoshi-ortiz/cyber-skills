---
purpose: the prompts that produced the generated reference beside them
admits: prompt JSON and the manifest naming which slice built which image
refuses: images, skill payload, any prompt no image in this tree came from
max_file_bytes: 70000
---

# Prompts

An image without its prompt cannot be varied, only replaced. These sit beside
the output so a round that wants "the same but wider" has something to edit.
