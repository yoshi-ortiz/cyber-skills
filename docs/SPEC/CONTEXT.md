---
purpose: promoted contracts, each one settled enough that a roadmap item can be judged against it
admits: one file per contract, naming its goal, its program, and its acceptance criteria
refuses: speculation, proposals, and anything still being argued -- those belong in GOAL.md's prototype backlog
---

# Promoted contracts

A file arrives here when the question behind it is answered and a `ROADMAP.md`
item can be closed against it. Each names the roadmap id that implements it, so
a contract with no indexed work is visible as such.
